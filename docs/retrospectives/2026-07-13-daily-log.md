---
date: 2026-07-13
datetime: 2026-07-13 23:46 JST
model: GPT-5 Codex
summary: |
  回収済み production 結果を summary HTML へ反映し、bad parameter 専用 HTML と図を追加した。
  L=4,U=16 の低温 NaN について、job failure ではなく長時間 trajectory 中の非有限値混入と
  finite check 不足による `status=ok` 伝播であることをローカル再現で切り分けた。
---

# 2026-07-13 Daily Log

## 今日完了したこと

- 回収済み production / diagnostic run を整理し、通常 summary HTML に反映した。
  - `docs/2026-07-03-temperature-summary-with-tables.html`
  - `scripts/update_recovered_production_summary.py`
  - `data/analysis/recovered_production_20260713/`
  - `docs/figures/recovered_production_20260713/`
- bad parameter 専用 HTML を作成し、失敗した parameter / temperature を明示した。
  - `docs/2026-07-13-bad-parameter-results.html`
  - `data/analysis/recovered_production_20260713/bad_parameter_groups.tsv`
  - `data/analysis/recovered_production_20260713/bad_parameter_results.tsv`
- 全 parameter について usable temperature window を通常 summary に反映した。
  - L=4,U=16 は `T >= 0.04` を usable とし、`T=0.0300075,0.02,0.01` は bad として除外。
  - bad がない parameter でも「収集済み範囲内で checks 通過」と分かるようにした。
- L=4,U=16 の低温 NaN 原因をローカルで切り分けた。
  - production では PP/APP とも beta=25 は finite、beta=33.325 以降は NaN。
  - `stab=2` diagnostic でも beta=33.325 以降は NaN で、stab interval 短縮だけでは改善しない。
  - ローカル MPI `nrep=4,nwarm=2000,nmeas=10000,nbin=100` で beta=33.325 の NaN を再現。
  - 個別 serial では seed `14012418791647386686` のみ NaN。
  - 同 seed は `nmeas=8000` では finite、`nmeas=9000` で NaN。
- 詳細レポートを docs に作成した。
  - `docs/2026-07-13-l4-u16-low-temperature-nan-report.md`
- 実行バイナリ同期ルールを作業規約へ反映した。
  - `AGENTS.md`
  - `CLAUDE.md` は `AGENTS.md` 参照のまま。
- 誤って同期された実行バイナリは削除済みで、以後は明示指定がない限り同期しない方針。

## 現時点の重要な判断

- L=4,U=16 の低温 NaN は PBS/MPI ジョブの異常終了ではない。
- NaN は長時間 trajectory 中に特定 seed で混入する。
- `replica_bin_values()` が `count` と `sum_sign==0` しか検査しないため、NaN sum が `status=ok` のまま jackknife に流れる。
- sign は 1 のままなので、今回の問題は sign denominator ではなく Green/observable の非有限化として扱う。
- 物理解析では、コード修正と再 run までは L=4,U=16 の beta=33.325 以下を使わない。

## 明日の Handoff

最初に読む:

- `docs/2026-07-13-l4-u16-low-temperature-nan-report.md`
- `LOG.md` の 2026-07-13 23:46 / 23:40 エントリ

最初に実装する:

1. NaN を `status=ok` にしない fail-fast finite check を入れる。
2. `measure_sample()` 後に `MeasSample` 全フィールドを `isfinite()` で検査する。
3. 測定直前に `D.Gu.g` / `D.Gd.g` の finite check を入れる。
4. `replica_bin_add()` または呼び出し側で non-finite sample/sign を拒否する。
5. `replica_bin_values()` で bin sums と出力値の finite check を行う。
6. `jackknife()` または `jackknife_and_print()` でも入力配列の finite check を行う。

主に触るファイル:

- `src/replica_run.c`
- `src/replica.c`
- `src/replica.h`
- `src/measure.c`
- `src/measure.h`
- `tests/test_mpi_replica.c`
- 必要なら新規 test file under `tests/`

追加したいテスト:

- `replica_bin_values()` が NaN sum を reject する。
- non-finite `MeasSample` が replica failure になる。
- MPI pack/unpack 後の NaN bin が root 側で failure になる。

fail-fast 実装後の再現条件:

```ini
lattice=file
latfile=data/production_runs/kugui_F1cpu_2d4x4_U16_PP_dtau0p025_T0p04_to_0p01_nrep120_n10k_gridT_alt_fc0416a_prebuilt_U12U16_20260707/hopping_PP.txt
Lx=4
Ly=4
pbc=1
t=-1.0
U=16
dtau=0.025
beta_list=33.325
nwarm=2000
nmeas=9000
nbin=100
stab=4
parallel=serial
nrep=1
profile=0
seed=14012418791647386686
sweep_order=alternating
```

実行:

```sh
./dqmc input.in
```

MPI-enabled build を使う場合:

```sh
mpirun -np 1 ./dqmc_mpi input.in
```

この条件は `nmeas=8000` では finite、`nmeas=9000` で NaN になる。fail-fast 後は最初の failure sweep/bin/measurement index を拾う。

次の段階:

- failure が Green 非有限なら、`src/linalg.c:udv_inv_one_plus_work()` に段階的 finite 診断を追加する。
- check points は `Tinv`, `UT_Tinv`, `M`, `Minv`, `tmp`, `gp`, `g`。
- exact operation が分かったら、`stab=1`, SVD fallback, scale handling 改善などを検討する。

## 注意事項

- リモート操作、同期、ジョブ投入は、明示の指示があるまで行わない。
- 実行バイナリは、明示指定がない限り同期しない。
- `data/production_runs/` 以下の大量の未追跡 run directory は回収済み結果なので削除しない。
- `data/analysis/recovered_production_20260713/` と `docs/figures/recovered_production_20260713/` は summary HTML の生成物。
- `/tmp/afqmc_l4u16_*` の診断ファイルは一時ファイル。必要な条件はこの日次ログと詳細レポートに写してある。

## 今日の検証

- `python3 scripts/update_recovered_production_summary.py` 実行成功。
- summary HTML / bad parameter HTML の相対リンク確認で missing 0。
- `git diff --check` OK。
- `__pycache__` なし。
